# Alta Outbound Voice Agent — pinned dependencies
# Every non-stdlib import is pinned with == (CLAUDE.md §1.1, ENV2/PKG1).
# Regenerate: python3.13 -m venv .venv && pip install fastapi uvicorn httpx pydantic python-dotenv pytest && pip freeze
# stdlib imports (os, sys, json, re, pathlib, datetime, decimal, enum, etc.) are NOT listed here.

# --- web framework + ASGI ---
fastapi==0.138.0
starlette==1.3.1
uvicorn==0.49.0

# --- HTTP client (Vapi REST adapter) ---
httpx==0.28.1
httpcore==1.0.9
h11==0.16.0

# --- data validation + models ---
pydantic==2.13.4
pydantic_core==2.46.4
annotated-types==0.7.0
typing_extensions==4.15.0
typing-inspection==0.4.2

# --- env / secret loading (loaded at runtime ONLY — never at import) ---
python-dotenv==1.2.2

# --- test runner ---
pytest==9.1.1
iniconfig==2.3.0
packaging==26.2
pluggy==1.6.0

# --- transitive deps ---
anyio==4.14.0
certifi==2026.6.17
click==8.4.1
idna==3.18
Pygments==2.20.0
annotated-doc==0.0.4
